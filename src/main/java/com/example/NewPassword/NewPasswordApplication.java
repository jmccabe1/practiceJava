package com.example.NewPassword;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class NewPasswordApplication { //This name is a relic from a time when I didn't care as much about names that made sense

	public static void main(String[] args) {
		SpringApplication.run(NewPasswordApplication.class, args);
	}
	//Here is where we start the Gradle daemon which executes the logic for the palindrome

}
